<!DOCTYPE HTML>
<html>
<head>
<title>Demo Express App</title>
<link rel= "stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
<link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
<link rel="stylesheet" href="/stylesheets/style.css">
<link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">YelpCamp</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
<% if(!currentUser) { %>
        <li class="nav-item log1"><a class="nav-link" href="/login">Login</a></li>
  <li class="nav-item log1"><a class="nav-link" href="/register">Sign Up</a></li>
<% } else { %>
  <span class="navbar-text">
       Hello <%= currentUser.username  %>
     </span>
  <li class="nav-item log2"><a class="nav-link" href="/logout">Log Out</a></li>
<% } %>
  </ul>

      </div>

  </nav>
  <div class="container">
<% if (error && error.length > 0) { %>
<div class="alert alert-danger" style="margin-top: 20px;" role="alert"><%= error %>  </div>
<% } if(success && success.length > 0) { %>
<div class="alert alert-success" style="margin-top: 20px;" role="alert"><%= success %>  </div>  <% } %>
</div>
